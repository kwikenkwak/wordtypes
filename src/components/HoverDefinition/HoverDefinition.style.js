import styled from 'styled-components'
import { base, bg } from 'utils/themeutils'

export const HoverDefinition = styled.div`
  padding: 10px;
  background-color: ${bg};
  border: 1px solid ${base};
  border-radius: 10px;
  color: ${base};
`
