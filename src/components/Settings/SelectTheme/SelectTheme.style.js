import styled from 'styled-components'

export const SelectTheme = styled.div`
  display: flex;
  margin: .2em 0;
`

// The title is a bit larger and
// bold
export const Title = styled.div`
  font-weight: bold;
  margin-right: auto;
`
