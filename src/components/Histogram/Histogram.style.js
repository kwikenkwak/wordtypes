import styled from 'styled-components'
import { clr } from 'utils/themeutils'

export const GridAxisWrapper = styled.div`
  stroke: ${clr('baseColorGraph')};
  fill: ${clr('baseColorGraph')};
  color: ${clr('baseColorGraph')};
`
