import styled from 'styled-components'
import { base } from 'utils/themeutils'

export const TextDone = styled.span`
`

export const CurrentWord = styled.span`
  text-decoration: underline 3px ${base};
`
export const TextTodo = styled.span`
`
